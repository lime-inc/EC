import { ExtensionTypes, UIExtensionTypes, ExternalExtensionTypes } from '../../constants.js';
import { AppInterface } from '../../models/app/app.js';
import { DependencyVersion } from '@shopify/cli-kit/node/node-package-manager';
interface ExtensionInitOptions<TExtensionTypes extends ExtensionTypes = ExtensionTypes, TExternalExtensionTypes extends ExternalExtensionTypes = ExternalExtensionTypes> {
    name: string;
    extensionType: TExtensionTypes;
    externalExtensionType: TExternalExtensionTypes;
    app: AppInterface;
    cloneUrl?: string;
    extensionFlavor?: string;
}
interface ExtensionDirectory {
    extensionDirectory: string;
}
declare type UIExtensionInitOptions = ExtensionInitOptions<UIExtensionTypes> & ExtensionDirectory;
declare function extensionInit(options: ExtensionInitOptions): Promise<string>;
export declare function getRuntimeDependencies({ extensionType, }: Pick<UIExtensionInitOptions, 'extensionType'>): DependencyVersion[];
export default extensionInit;
